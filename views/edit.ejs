<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/styles.css">
</head>
<body>
<div class="w-full h-full bg-zinc-900 p-10 text-white">
    <div class="nav mb-10">
        <a class="text-blue-500" href="/read">Read users</a>
    </div>
<h1 class="text-3xl tracking tighter mb-3">create user</h1>
<form action="/update/<%= user._id %>" method="POST" class="flexwrap ">
    <input type="text" name="name" placeholder="Name" value="<%= user.name%>" class="px-5 py-2 rounded-md bg-zinc-800 border-2 border-zinc-700">
    <input type="email" name="email" placeholder="Email" value="<%= user.email%>" class="p-2 rounded-md bg-zinc-800 border border-zinc-700">
    <input type="text" name="image" placeholder="Image url" value="<%= user.image%>" class="p-2 rounded-md bg-zinc-800 border border-zinc-700">
    <button type="submit"  value="update user" class="bg-yellow-600 text-black p-2 rounded-md">Update User</button>
</form>
</div>
</body>
</html>